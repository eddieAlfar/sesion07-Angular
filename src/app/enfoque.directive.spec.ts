import { EnfoqueDirective } from './enfoque.directive';

describe('EnfoqueDirective', () => {
  it('should create an instance', () => {
    const directive = new EnfoqueDirective();
    expect(directive).toBeTruthy();
  });
});
